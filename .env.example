# Domain Configuration
# Your application domain (used for generating absolute URLs)
# For local development, use http://localhost:3000
# For production, use your actual domain (e.g., https://yourdomain.com)
# Vercel automatically provides VERCEL_URL, so DOMAIN is optional if deploying on Vercel
DOMAIN=http://localhost:3000

# Email Configuration (Nodemailer SMTP)
# Required for contact form functionality

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Authentication
# For Gmail, you'll need to generate an App Password instead of your regular password
# See: https://support.google.com/accounts/answer/185833
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Email Addresses
# Address that will appear as the sender
APPLICATION_FROM_ADDRESS=noreply@example.com
# Address where contact form submissions will be sent
APPLICATION_TO_ADDRESS=contact@example.com
